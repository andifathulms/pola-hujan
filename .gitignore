node_modules
.next
out
.preview
# Generated by `pnpm data:build`; never committed (mirrors the "no raw
# grids committed" rule — see CLAUDE.md invariant 13). Regenerate locally
# before `pnpm dev` or `pnpm build`.
data/grids
.DS_Store
*.tsbuildinfo
.env
.env.local
coverage
# Raw brand-kit export bundle (many sizes/variants of the same marks).
# The specific files actually used by the app or the README are copied
# into app/, public/, and .github/ instead of referencing this folder.
exports
