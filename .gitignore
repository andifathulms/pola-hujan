node_modules
.next
out
# Generated by `pnpm data:build`; never committed (mirrors the "no raw
# grids committed" rule — see CLAUDE.md invariant 13). Regenerate locally
# before `pnpm dev` or `pnpm build`.
data/grids
.DS_Store
*.tsbuildinfo
.env
.env.local
coverage
